
# Port the backend listens on (local dev).
PORT=8080

# Directory to recursively load rule YAML from, relative to the backend/ dir
# where you run the server (local dev).
RULES_PATH=../rules

# Comma-separated list of origins allowed by CORS.
ALLOWED_ORIGINS=http://localhost:3000

# Environment label used in logs: development | production
ENV=development
